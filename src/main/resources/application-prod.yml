spring:
    datasource:
        url: jdbc:mysql://${DB_HOST:192.168.1.2}:${DB_PORT:3310}/${DB_NAME:nookx_ingester}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

ingester:
    ingest-targets:
        klickypedia-sets:
            base-url: ${NOOKX_BASE_URL:https://qa.nookx.app}
